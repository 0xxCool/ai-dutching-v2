# ==========================================================
# AI DUTCHING SYSTEM - DEPENDENCIES
# ==========================================================

# Core Dependencies
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
requests>=2.31.0
python-dotenv>=1.0.0
tqdm>=4.65.0
pyyaml>=6.0  # For config.yaml

# Machine Learning
scikit-learn>=1.3.0
xgboost>=2.0.0
lightgbm>=4.0.0

# Deep Learning (for Neural Networks & Cashout DQN)
# WICHTIG: Für GPU-Support (RTX 3090) siehe GPU Installation Sektion unten!
torch>=2.0.0  # Mit CUDA 11.8 oder 12.1 für GPU
torchvision>=0.15.0

# API & Caching
aiohttp>=3.8.0  # Async HTTP
diskcache>=5.6.0  # File-based caching
redis>=4.6.0  # Optional: Redis caching

# Dashboard & Visualization (REQUIRED for dashboard.py)
streamlit>=1.25.0
plotly>=5.15.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Database (Optional but recommended)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0  # PostgreSQL
alembic>=1.11.0  # Migrations

# Logging & Monitoring
loguru>=0.7.0

# Data Validation
pydantic>=2.0.0

# Utils
rapidfuzz>=3.0.0  # Fast string matching
python-telegram-bot>=20.0  # Optional: Telegram alerts
discord-webhook>=1.0.0  # Optional: Discord alerts

# Development (Optional)
black>=23.0.0  # Code formatting
pytest>=7.4.0  # Testing
jupyter>=1.0.0  # Notebooks
ipykernel>=6.25.0

# Optimization (Optional)
optuna>=3.0.0  # Hyperparameter tuning
mlflow>=2.7.0  # Model registry

# GPU-Monitoring (für RTX 3090 Performance-Tracking)
nvidia-ml-py3>=7.352.0  # NVML Python Bindings

# ==========================================================
# GPU INSTALLATION (RTX 3090 OPTIMIERT)
# ==========================================================
#
# WICHTIG: Für maximale Performance mit Nvidia RTX 3090:
#
# 1. CUDA Toolkit installieren:
#    - Download: https://developer.nvidia.com/cuda-downloads
#    - Empfohlen: CUDA 12.1 oder CUDA 11.8
#    - Installiere: CUDA Toolkit + cuDNN
#
# 2. PyTorch mit CUDA installieren:
#
#    Für CUDA 12.1 (Empfohlen für RTX 3090):
#      pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
#    Für CUDA 11.8:
#      pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
# 3. XGBoost mit GPU-Support:
#    pip install xgboost --config-settings use_cuda=True
#
# 4. Verifiziere GPU:
#    python -c "import torch; print(f'CUDA Available: {torch.cuda.is_available()}'); print(f'GPU: {torch.cuda.get_device_name(0) if torch.cuda.is_available() else None}')"
#
# 5. Teste RTX 3090 Performance:
#    python gpu_ml_models.py
#
# ==========================================================
# INSTALLATION GUIDE
# ==========================================================
#
# WINDOWS SERVER INSTALLATION:
# ----------------------------
# 1. Installiere Python 3.10 oder 3.11 (64-bit)
#
# 2. Installiere CUDA Toolkit (siehe GPU Installation oben)
#
# 3. Installiere Core Dependencies:
#    pip install pandas numpy scipy requests python-dotenv tqdm pyyaml
#    pip install streamlit plotly matplotlib seaborn
#
# 4. Installiere PyTorch mit CUDA:
#    pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# 5. Installiere Rest:
#    pip install -r requirements.txt
#
# 6. Optional - GPU Monitoring:
#    pip install nvidia-ml-py3
#
# MINIMAL (Core + Dashboard):
# ---------------------------
#   pip install pandas numpy scipy requests python-dotenv tqdm pyyaml
#   pip install streamlit plotly matplotlib
#
# RECOMMENDED (mit ML):
# ---------------------
#   pip install -r requirements.txt
#
# FULL GPU SYSTEM (maximale Performance):
# ---------------------------------------
#   # 1. CUDA Toolkit installieren
#   # 2. PyTorch mit CUDA:
#   pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   # 3. Alle Dependencies:
#   pip install -r requirements.txt
#   # 4. Optional:
#   pip install redis psycopg2-binary python-telegram-bot discord-webhook nvidia-ml-py3
#
# ==========================================================
# PERFORMANCE-ERWARTUNGEN (RTX 3090)
# ==========================================================
#
# Neural Network Training:
# - CPU: ~100-200 samples/sec
# - RTX 3090 (FP32): ~1000-2000 samples/sec
# - RTX 3090 (FP16 Mixed Precision): ~2000-4000 samples/sec
# → 10-40x Speedup!
#
# XGBoost Training:
# - CPU: Baseline
# - RTX 3090 GPU: 10-50x schneller
#
# Inference (Prediction):
# - Batch-Predictions: 100-500x schneller
#
# Deep RL Training (DQN):
# - CPU: ~50-100 steps/sec
# - RTX 3090: ~1000-3000 steps/sec
# → 20-60x Speedup!
#
# Erwartete VRAM-Nutzung:
# - Neural Network: ~500-2000 MB
# - XGBoost: ~1000-3000 MB
# - Deep RL DQN: ~1000-4000 MB
# - Gesamt-System: ~5-10 GB (von 24 GB RTX 3090)
# → Reichlich Reserve für große Batches!
#
# ==========================================================
