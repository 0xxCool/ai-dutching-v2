# ==========================================================
# AI DUTCHING SYSTEM - ENVIRONMENT VARIABLES
# ==========================================================

# Sportmonks API Token (REQUIRED)
# Get from: https://www.sportmonks.com/
# Required Plan: European Standard + xG Add-on (€80/month)
SPORTMONKS_API_TOKEN=your_api_token_here

# ==========================================================
# OPTIONAL: TELEGRAM ALERTS
# ==========================================================
# 1. Create bot via @BotFather on Telegram
# 2. Get bot token
# 3. Send /start to your bot
# 4. Get chat_id via: https://api.telegram.org/bot{TOKEN}/getUpdates

TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_CHAT_ID=your_telegram_chat_id

# ==========================================================
# OPTIONAL: DISCORD ALERTS
# ==========================================================
# 1. Server Settings → Integrations → Webhooks
# 2. Create Webhook
# 3. Copy Webhook URL

DISCORD_WEBHOOK_URL=your_discord_webhook_url

# ==========================================================
# OPTIONAL: EMAIL ALERTS
# ==========================================================
# For Gmail: Use App Password, not regular password!
# Google Account → Security → 2-Step Verification → App Passwords

EMAIL_SMTP_SERVER=smtp.gmail.com
EMAIL_SMTP_PORT=587
EMAIL_SENDER=your.email@gmail.com
EMAIL_PASSWORD=your_app_password
EMAIL_RECIPIENT=recipient@example.com

# ==========================================================
# OPTIONAL: DATABASE (PostgreSQL)
# ==========================================================
# Only needed if migrating from CSV to PostgreSQL

DATABASE_URL=postgresql://user:password@localhost:5432/dutching

# ==========================================================
# OPTIONAL: REDIS CACHE
# ==========================================================
# Only needed if using Redis instead of file-based cache

REDIS_URL=redis://localhost:6379/0

# ==========================================================
# INSTRUCTIONS
# ==========================================================
# 1. Copy this file to .env
#    cp .env.example .env
#
# 2. Fill in your credentials
#
# 3. NEVER commit .env to git!
#    (.env is in .gitignore)
#
# ==========================================================
